// first element of selects
var _AUCUN = "---- Aucun ----";
// json source file generated by excel
var _EXCELSRC = [];
// Get actual years
var _YEAR = new Date().toLocaleDateString().split('/').reverse()[0];
// form datas generated
var _DATAS = {};
// popup params print
const _PARAMPRINT = `scrollbars=no,resizable=no,status=no,location=no,toolbar=no,menubar=no,width=750,height=500,left=10,top=10`;

var _DATE = undefined;

var rpgReferences = {};

var _STORE = {}; 

var _NOTYET = "Not implemented yet";

const createContext = () => {
    let mode = "new";
    if (window.location.href.includes('?')) {
        ["id", "selection", "excel", "after", "aliquote", "new", "echantillon"].forEach(e => {
            if (isKeyInUrl(e)) setContext(e, +getNumberFromUrl(e), mode);            
        }); 
    } else setContext("new", 0);
    return getContext();
}

const getContext = () => {
    const elem = getElement("ctx");
    if (elem)  return {
        id: elem.value,
        mode: elem.getAttribute("name"),
    }
    return undefined
}

const setContext = (name, id)  => {
    const elem = getElement("ctx");
    // for refresh before actions
    _DATAS = formToJSON(document.getElementsByClassName('formData')[0].elements); 

    if (elem) {
        elem.value = +id;
        elem.setAttribute("name", name);
        return getContext();
    }
    return undefined
}

const isContextMode = (input) => (typeof input === 'string') ? [input].includes(getElement("ctx").getAttribute("name")) :  Boolean(input.includes(getElement("ctx").getAttribute("name")));

// test if key in url
isKeyInUrl= (keyName) => window.location.href.includes(`?${keyName}=`) || window.location.href.includes(`&${keyName}=`);

// return key value number
function getNumberFromUrl(keyName) {
    const tmp = window.location.href.split('?')[1].split("&").filter(key =>  key.includes(keyName+'='))[0];
    return +tmp.split(`=`)[1] || 0;
}

function getFromUrl(keyName) {
    const tmp = window.location.href.split('?')[1].split("&").filter(key =>  key.includes(keyName+'='))[0];
    return tmp.split(`=`)[1] || '';
}

formatDate = (input) => input && input.includes("T") && input.includes("-") ? input.split("T")[0].split("-").reverse().join('-') : input;


function createBlankKeySticker(name, value) {
    return {
        "key": value || "type",
        "align": "center",
        "size": "12px"
    }
}

function getTemplateSticker(values) {
    return {
        etiquette : getElement("etiquette").value ? JSON.parse(getElement("etiquette").value) : _CONFIGURATION.etiquette,
        ... values
    }
}

function changeValueInJson(name, key, value) {
    const tmp = toJson(name);
    if (typeof key === "string")
        tmp[key] = value;
    else 
        tmp[key[0]][key[1]] = value;
    
    getElement(name).value = JSON.stringify(tmp);
};



// function setDatas() {
// 	localStorage.setItem('trace', _DATAS);
// }

// function getDatas(themeName) {
//     localStorage.setItem('trace', _DATAS);
//     if (localStorage.getItem('trace')) {
//         looadDatas(localStorage.getItem('trace'))
//     }
// }

// function clearDatas() {
//     localStorage.clear();
// }








